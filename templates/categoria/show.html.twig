{% extends '@AdminLTE/layout/default-layout.html.twig' %}

{% block page_title %} Categoria {% endblock %}
{% block page_subtitle %}  Eliminar {% endblock %}

{% block page_content_before %}
  
  <div class="marker">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categorium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoria_index') }}">back to list</a>

    <a href="{{ path('categoria_edit', {'id': categorium.id}) }}">edit</a>

    {{ include('categoria/_delete_form.html.twig') }}
    </div>
{% endblock %}
