{% extends '@AdminLTE/layout/default-layout.html.twig' %}

{% block page_title %} Cabaña {% endblock %}
{% block page_subtitle %}  Eliminar {% endblock %}

{% block page_content_before %}
  
  <div class="marker">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cabana_entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cabana_entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cabana_entity_index') }}">back to list</a>

    <a href="{{ path('cabanaentity_edit', {'id': cabana_entity.id}) }}">edit</a>

    {{ include('cabana_entity/_delete_form.html.twig') }}
    </div>
{% endblock %}
