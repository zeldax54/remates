<div class="marker">
<form method="post" action="{{ path('cabana_delete', {'id': cabana.id}) }}" onsubmit="{{ deletemsg }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cabana.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
</div>